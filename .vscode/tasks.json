{
  // VSCode task definitions.
  "version": "2.0.0",
  "tasks": [
    {
      // Prepare venv and development certificate before debugging.
      "label": "otomekairo: prepare debug env",
      "type": "shell",
      "command": "${workspaceFolder}/scripts/prepare_debug_env.sh",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}