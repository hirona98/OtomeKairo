# 現行計画

## この文書の役割

この文書は、OtomeKairo の現在地、現行基盤の到達点、残っている拡張テーマだけを簡潔に管理する。

- 設計判断は `docs/design/` を正とする
- 実装状態はコードを正とする
- 実装や仕様を変えたら、同じ変更内で docs も更新する
- 旧仕様互換だけの移行レイヤーや検索フォールバックは扱わない

## 現在地

現在の実装は、主要な会話基盤と観測基盤までは通っている。

- 会話基盤:
  通常会話 1 サイクル、通常会話の任意画像入力、bootstrap、設定取得、設定変更、catalog、判断サイクル inspection API
- 記憶基盤:
  `SQLite + sqlite-vec`、`turn consolidation`、`RecallHint`、`memory_interpretation`、`RecallPack`、`reflective consolidation`、`drive_state` 第一段、非会話サイクルの選択的 episode 化
- 自発判断基盤:
  runtime-only の `pending_intent` キュー、wake observation API、background 起床スケジューラ、`reply / noop / pending_intent`、`world_state` 第一段の前景参照
- 観測基盤:
  `desktop_watch` 第一段、`client_context` の正規化、`vision.capture` capability、`vision.capture` 対応 client 自動選択、画像観測要約の判断入力反映、capability availability inspection、`logs/stream`
- 外界状態基盤:
  `world_state` 第一段、`client_context` / `desktop_watch` 観測要約 / capability result summary からの短期外界状態更新、foreground summary の判断入力反映、inspection の `world_state_trace`
- 後段処理:
  durable な memory postprocess worker、`status.runtime_summary`、inspection の `memory_trace`、`ongoing_action_exists`
- 運用確認:
  `scripts/run_long_smoke.py` による `smoke / soak` 実行、isolated data dir、主要な capture failure case の確認
- 意味判断での LLM 採用:
  `reflective consolidation` の `summary_text`、`event_evidence`、`RecallPack` の意味的選別、pending-intent 候補選択、`vision.capture` と通常会話画像の観測要約、`capability_decision_view`、`initiative_context` 第一段 summary

この前提では、会話、記憶、自発判断、`desktop_watch` 方針と `vision.capture` capability の第一段、`drive_state` 第一段、長時間 smoke までを現行基盤の到達点として扱う。
これは設計上の完成を意味しない。
以降は、設計上の完成形との差分を埋める拡張段階として扱う。

## 既知の制約

次は現時点の設計上の制約であり、未整理のバグ一覧ではない。

- `event_evidence` は短い圧縮根拠であり、逐語引用や正確引用は保証しない
- `RecallHint` は長期記憶を読まず、現在入力と直近文脈だけから作る
- `RecallHint` は `interaction_mode` と `primary_recall_focus` を主軸にし、完全な同時最適化は行わない
- 通常会話 API は `text` と `client_context` に加えて任意の `images` を受けられるが、raw image payload の保存や OCR 全文抽出はまだ行わない
- `desktop_watch` は `vision.capture` の image payload を短い観測要約へ変換して判断へ入れるが、raw image payload の保存、OCR 全文抽出、UI 構造化はまだ行わない
- `wake` と `desktop_watch` は共有判断パイプラインへ入り、`reply`、保留意図更新、capability failure、意味のある `world_state` 変化が起きたサイクルだけ `turn consolidation` を走らせる。条件に当てはまらない自律・観測 `noop` は `memory_trace=skipped` に留める
- `world_state` は `client_context` と capability result summary から短期状態を更新する第一段までであり、外部サービス、身体、機器、予定の専用 source pack 最適化は後続拡張で扱う
- `world_state` は foreground summary を判断入力へ渡すが、生の画像、音声、長い payload は直接渡さず、必要なら短い補助要約だけを使う
- 個別記憶行は OtomeKairo 自身の記憶更新処理だけが変更し、管理 API、retention 実行、redaction 実行、export は追加しない
- `memory_set` 全体の clone/delete は設定定義操作として扱い、UI の未確定操作は client 側の下書きで保持する
- `editor-state` の全体置換は、記憶集合の実体 clone/delete を暗黙実行しない
- `editor-state` の秘密値返却例外は認証済み編集操作として扱い、read/write の audit event を秘密値なしで記録する
- background wake は複雑な時間帯制御や外界行動実行をまだ持たない
- 自律判断はまだ `pending_intent` の再評価中心であり、`drive_state` は `reflective consolidation` から作る第一段だけで、生成品質と活用範囲はまだ限定的である
- 自律判断入力には `initiative_context` 第一段 summary を入れるが、`pending_intent` が空の状況でも `drive_state` と現在文脈から自発系評価へ進む initiative loop はまだ後続拡張で扱う
- `ongoing_action` は `vision.capture` request / response に対する第一段の作成 / 完了 / timeout 終了までは入っているが、汎用 capability 実行や複数段の継続更新にはまだ一般化していない
- capability manifest、binding、`capability_state` の責務分離は `vision.capture` の availability inspection まで実装済みであり、能力ファミリー全体の実装一般化は後続拡張で扱う
- 回帰テスト整備は現行計画の完了条件に含めない

## 直近設計変更の実装同期

次は docs 側の設計を更新済みであり、コード同期時に確認する。

- bootstrap:
  `register-first-console` は token 未発行時だけ発行し、発行済み状態では既存 token を返さない
- capability:
  `CapabilityManifest / CapabilityBinding / CapabilityState` の分離と availability の意味境界は [../design/17_capability_manifest.md](../design/17_capability_manifest.md) を正とし、コード同期時にその境界を確認する
- memory interpretation:
  `candidate_memory_units` は意味骨格 hint を含む記憶候補メモに留め、半構造化した `memory_units` 正本はコードが作る

## 次の拡張

現時点で保留している主要な拡張は次である。

- capability manifest、binding、`capability_state` の分離を汎用 capability 実行へ反映する
- `ongoing_action` を汎用 capability 実行へ広げ、継続 / 完了 / 中断の状態遷移を一般化する
- `world_state` の対象 source を外部サービス、身体、機器、予定へ広げ、state_type ごとの source pack と TTL を細かくする
- `relationship / self` 要約品質を改善する
- 人格設定と記憶から派生する `drive_state` の生成品質と活用範囲を拡張し、自律判断を短期候補再評価だけに寄せないようにする
- `pending_intent` が無い場合の自発系評価を `initiative_context` と `drive_state` から扱う
- 時刻帯や前景状況を使う判断補助を絞って足す
- `events` の限定ロードを使う精密根拠確認を追加する
- `--profile soak` を土台に、より長い実時間運用と追加 failure case を確認する

上の拡張は後続の拡張テーマとして扱い、現行基盤の到達点とは分ける。
