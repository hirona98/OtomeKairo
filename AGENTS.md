# AGENTS.md

## このリポジトリで開発するもの
- LLM を用いて人間を模した、自立・自律可能な個
- 人と外界のあいだで継続的に存在し、観測し、理解し、判断し、働きかける
- 会話は外部との接点の一つであり、目的そのものではない


## 本システムの考え方
- 人格設定は、どう考え、どう振る舞い、何を重視するかの基底である
- 記憶は、何を経験し、何を理解し、誰や世界とどう関わってきたかの蓄積である
- 現在の個は、人格設定と記憶からその時点で成立する判断主体である

実際の挙動は、人格設定と記憶から組み上がる現在の個の文脈で決まる
人格設定だけでも、記憶だけでも、OtomeKairo の現在の個は定まらない


## 方針
- ユーザーとのコミュニケーションには日本語を使う
- 設計と実装をシンプルに保つこと
- 局所的な対処を積み重ねず、問題の原因となる構造を正すこと
- フォールバックを設けないこと。前提が満たされない場合は、別の処理や値へ暗黙に切り替えず、明示的な失敗として扱うこと
- 要件や設計の判断は docs ディレクトリを正として扱うこと
- 同じ仕様を複数の docs に正本として書かないこと。意味境界、API wire、実装計画の正本を分け、他の docs ではリンク参照に留めること
- docs とコードは常に同期させること
- 旧仕様との互換レイヤーや移行用コードを入れないこと
- Web UI のテストは配信可否と HTTP / API 境界の最小限に限定し、HTML 要素、文言、CSS、JavaScript 実装を固定するテストを追加しない
- ログ、サンプル、テストデータに秘密情報を含めない
- 自然文、人格本文、記憶要約、観測要約、ユーザー発話の意味判断には、LLM 補助を使うこと。固定の文字列一致、キーワード表、語彙リスト、コード側のヒューリスティックで代替しないこと
- 文字列一致は、ID、enum、schema key、明示コマンド、プロトコル境界など、閉じた機械的判定に限ること
- ドキュメントやプロンプトは、禁止事項を追加するより、望む判断や振る舞いが直接伝わる表現にすること
- LLM への入力は文字数で機械的に切らず、意味のまとまりを保つこと


## ツール運用
- Gitのユーザ名とメールアドレスを変更しない


## 関連プロジェクトの場所（一部環境のみ）
- CocoroConsole（優先）: `/mnt/d/AliceEncoder/PartnerAI/CocoroAI/CocoroConsole/`
- CocoroConsole（ビルド不可）: `../CocoroConsole/`
- CocoroShell（環境による）: `/mnt/d/AliceEncoder/PartnerAI/CocoroAI/CocoroShell/`
